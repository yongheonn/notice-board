<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yongheon.backend.DAO.RegisterDAO">
	
	<select id="isExistId" resultType="String">
		select id from user where id = #{id}
	</select>

	<select id="isExistNick" resultType="String">
		select nick from user where nick = #{nick}
	</select>

	<select id="isExistEmail" resultType="String">
		select email from user where email = #{email}
	</select>
	
	<insert id="regi">
	 	INSERT INTO 
	 	user(
	 		id,
	 		pw,
	 		nick,
	 		email
	 	) 
	 	VALUES(
	 		#{id},
	 		#{pw},
	 		#{nick},
	 		#{email}
	 	) 
	</insert>
</mapper>