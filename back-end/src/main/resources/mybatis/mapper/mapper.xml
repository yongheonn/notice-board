<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.user.mappers">
	
	<select id="isExistId" resultType="String">
		select
        ifnull(id, "")
         id from user where id = #{id}
	</select>
	
	<insert id="regi">
	 	INSERT INTO 
	 	user(
	 		id,
	 		pw,
	 		nick,
	 		email
	 	) 
	 	VALUES(
	 		#{id},
	 		#{pw},
	 		#{nick},
	 		#{email}
	 	) 
	</insert>
</mapper>